# CONTRIBUTING

このリポジトリへの貢献方法です。データの収録原則そのものは [README.md](./README.md#収録の原則) と
[DESIGN.md](./DESIGN.md) を参照してください。ここでは「実際に PR をどう書くか」を扱います。

### はじめての方へ

名前を1件追加・訂正するだけなら、下の「PR の書き方」を上から順に読めば大丈夫です。
PR に慣れていない場合は、GitHub の Issue から申請していただく形でも構いません（README の
「削除・訂正申請」参照）。

## PR の書き方（個人勢・少数の追加/訂正）

1. `data/entries.tsv` に 1 行追加します（既存の行の下に足せば OK です。順序に意味はありません）。
2. **出典 URL（`source_url`）をお願いします。** 読みを実際に確認できる URL（本人チャンネル・本人 X・
   所属組織の公式ページ等）を入れてください。あとから読みを確かめ直せるようにしたいので、
   ここだけは省略できません。
3. `reading_source` は [DESIGN.md の「reading_source の種別」表](./DESIGN.md#データモデル)
   から選びます。信頼度の高い順に:

   | 値 | 選ぶ場面 |
   |---|---|
   | `org_kana` | 所属組織の公式一覧・個別ページに公式のかな読みが載っている |
   | `kana_surface` | 表記自体がひらがな・カタカナのみ（読み＝表記） |
   | `self_channel_title` | 本人のチャンネル名にかな読みが併記されている（例: `k4sen / かせん`） |
   | `self_profile` | 本人のチャンネル説明文・本人チャンネルの動画タイトルでの自己言及・X プロフィール |
   | `wikidata` | Wikidata の「かな表記」（P1814）が設定されている |
   | `wikipedia` | 日本語版 Wikipedia の記事本文に読みがあり、脚注の出典が付いている（出典の無い記事は使わないでください） |
   | `org_romaji` | 公式一覧はローマ字のみで、それを人（あなた）が出典を見てかな読みに確定した |
   | `pr_manual` | 上記のどれにも当てはまらないが、出典を示せる |

   迷ったら、より上（信頼度が高い方）の値に対応する出典があればそちらを優先していただけると助かります。
   例えば公式一覧にローマ字しか無くても、本人チャンネルの説明文に自己申告のかな読みがあれば
   `self_profile` の方が正確です。

4. `org_romaji` を選んだ場合は、`note` 列に読みを確定した経緯を書いてください
   （例: `note: 公式サイトのローマ字表記から確定（PR #123 レビュー時）`）。あとから見返せるようにするための一言です。
   機械的なローマ字→かな変換の結果をそのまま `reading` にしないでください（人が出典で確かめたものだけ）。
5. 表記（`surface`）は出典に書かれている通りの大文字・小文字をそのまま使ってください
   （例: `K4SEN` ではなく `k4sen`。YouTube・所属組織とも小文字表記が公式です）。
6. PR を送る前に、可能であれば手元で `python -m pytest` と `python build.py` を実行してみてください
   （CI でも自動的に走るので、手元での実行は必須ではありません）。

## 愛称・表記揺れの PR（`data/aliases.tsv`）

`data/entries.tsv` とは別に、出典を求めない愛称・表記揺れを `data/aliases.tsv` に収録しています。
PR を作れない方は Issue の
[愛称・略称の追加希望](../../issues/new?template=alias-addition.yml) からも申請できます
（出典欄はありません）。

1. `data/aliases.tsv` に 1 行追加します（列: `person_id` / `surface` / `reading` / `kind` / `note` / `added`）。
2. `person_id` は `data/entries.tsv` に既にある人物のものを使ってください（このファイルだけで
   新しい人物を作ることはできません）。
3. `kind` は次のいずれかです（詳しくは [DESIGN.md](./DESIGN.md) の aliases 節）:
   - `nickname`（表記が正式名と異なる愛称）
   - `reading_variant`（表記は正式名と同じで読みだけ別）
   - `family_name`／`given_name`（正式名の姓の部分／名の部分だけ）
   - `short_name`（本人が名乗る短縮名の読みで正式名を変換する。`surface` は正式名そのもの・`reading` は短縮名の読み）

   `reading_variant`・`short_name` を選んだ場合、`surface` は `data/entries.tsv` の当該人物の表記と
   一致させてください。`family_name`／`given_name` の `surface` は当該人物の表記の一部（部分文字列）に
   してください（どちらも満たさないと検証エラーになります）。`surface` と `reading` がともに 1 文字の行は
   登録できません。
4. こちらは出典 URL は不要です（愛称・表記揺れはどこまで確認できるか難しいことが多いため求めていません）。
5. 本人が望まない愛称は、正式名の削除申請（Issue）と同じ手続きで消せます。`person_id` 単位で扱うため、
   正式名が削除されるとその人物の愛称・表記揺れも自動的に配布物から除外されます。

## 大きめの変更について

大きめの変更（列の追加・形式の変更など）は先に Issue で相談してもらえると助かります。

## レビュー観点

PR をレビューする際は次を確認します:

- 出典 URL を実際に開いて、読みがそこに書かれている通りかを実際に開いて確認します（PR の記述だけで判断しません）。
- `reading_source` が実態と合っているか（例: 公式一覧に無いのに `org_kana` になっていないか）。
- `python -m pytest` と `python build.py` が通るか（CI が自動で確認しますが、ローカルでも
  確認できると早いです）。
- 削除・訂正申請（Issue 経由）は本人性の確認手段が書かれているか。ただし
  [README「削除・訂正申請」](./README.md#削除訂正申請) の通り、過剰な本人確認は求めません。
