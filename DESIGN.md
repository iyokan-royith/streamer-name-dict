# 配信者名前辞書（IME 変換辞書）DESIGN

配信者の活動名は読みが分かりにくいものが多く、IME 辞書として公開する。読みは本人・所属組織の
公式情報、または出典の明示された公開文献（Wikipedia・Wikidata）に由来するものだけを、出典 URL
付きで収録し、本人・組織からの削除・訂正申請は無条件で受ける。

## 目的

**配信者の活動名を IME で変換できる辞書を、出典付きで GitHub に公開し、みんなで育てる。**

## 収録の原則（README に転記する）

1. **活動名のみ**。本名・旧名義は載せない（本人が現在名乗っている名前だけ）
2. **読みは本人・所属組織の公式情報、または出典の明示された公開文献（Wikipedia・Wikidata）に
   由来するものだけ**。推定した読みは載せない
3. **全件に出典 URL を必ず入れる**（あとから確かめ直せるようにするため。`data/entries.tsv` のみ）。
   `data/aliases.tsv`（愛称・表記揺れ）は出典列を持たず、この原則の対象外（下記「aliases」節参照）
4. **削除・訂正申請は本人または所属組織からなら無条件・即時**。理由は問わない。
   削除後は配布物に含めない（`data/` の管理用の行は `status=removed` として残る）
5. **データは CC0 1.0（LICENSE-DATA）で公開する**

## 対象範囲

| 含む | 含まない |
|---|---|
| VTuber 事務所所属（にじさんじ・ホロライブ・ぶいすぽ・VOMS・Re:AcT …） | 事務所・グループ名そのもの（人名辞書に絞る） |
| ストリーマー・e スポーツ組織所属（ZETA・CR・RIDDLE …） | 現在活動していない方（載せない。既に載っている場合は削除申請と同じ扱いで配布物から外す） |
| 個人勢（本人または代理の PR で追加） | 読みが確定しない人 |

## データモデル

単一の真実は `data/entries.tsv`（1 表記 1 行。同一人物の別表記は別行で `person_id` で束ねる）。

| 列 | 例 | 意味 |
|---|---|---|
| `person_id` | `zeta-k4sen` | 人物の識別子（組織 slug + 活動名 slug）。削除申請の単位 |
| `surface` | `k4sen` | 変換後の表記。**出典に書かれた通りの大小文字**（K4SEN ではない。YouTube・ZETA とも小文字が公式） |
| `reading` | `かせん` | ひらがな読み |
| `reading_source` | `self_channel_title` | 読みの出典種別（下表） |
| `source_url` | `https://www.youtube.com/@k4sen` | 読みを確認した URL |
| `org` | `ZETA DIVISION` | 所属（個人勢は空） |
| `org_source_url` | `https://zetadivision.com/...` | 所属を確認した URL |
| `status` | `active` / `removed` | removed は配布物から除外 |
| `added` | `2026-09-07` | 登録日 |
| `note` | | 自由記述 |

**`reading_source` の種別（信頼度順・上ほど高い）**

> 順序の意味は「**読みを誰が確定したか**」——組織の公式かな ＞ 表記自体がかな ＞ 本人の自己申告 ＞
> 出典の明示された公開文献 ＞ 公式ローマ字を人が確認 ＞ PR で人が出典付き登録。

| 値 | 意味 | 人の確認 |
|---|---|---|
| `org_kana` | 所属組織の公式一覧・個別ページにかな読み | 不要 |
| `kana_surface` | 表記自体がかなのみ（読み＝表記） | 不要 |
| `self_channel_title` | 本人のチャンネル名に併記（`k4sen / かせん`） | 不要 |
| `self_profile` | 本人のチャンネル説明文・**本人チャンネルの動画タイトルでの自己言及**・X プロフィール（本人が自分の読みを書いたもの） | 不要 |
| `wikidata` | Wikidata の「かな表記」（P1814）。QID を `source_url` に | 不要 |
| `wikipedia` | 日本語版 Wikipedia の記事本文の読み（脚注の出典が付いているもの）。記事 URL を `source_url` に。出典無しの記事は使わない | 不要（出典付きなら） |
| `org_romaji` | 公式一覧にローマ字のみ → 公式のローマ字表記を、人が出典を見てかな読みに確定する。人が確認したことを `note` に書く（例: 「公式サイトのローマ字表記から確定」） | **必要** |
| `pr_manual` | PR で人が出典付きで登録 | レビューで出典を見る |

> **別表記（英字等）の行は元の行の `reading_source` を引き継ぐ。ただし `kana_surface` は
> `org_kana` に読み替える**——`kana_surface`（表記自体がかなのみ）は英字等の別表記行には
> 当てはまらないため（読みの根拠は「公式一覧に載っているかな名」）。

> **`wikipedia` の出典有無の判定単位**: **記事に読みの出典（脚注）があればよい。読みの行に直接
> `ref` が付いている必要はない**（メンバー表の節末にまとめて出典がある形を含む）。

### aliases（愛称・表記揺れ／出典を求めない補助情報）

`data/entries.tsv` は「公式の読み・出典あり」の行なので、出典の無い愛称・表記揺れをそこに混ぜない。
別ファイル `data/aliases.tsv` に分ける。単一の真実は `data/aliases.tsv`。**出典列を持たない**。

| 列 | 例 | 意味 |
|---|---|---|
| `person_id` | `hololive-yuzukichoco` | `data/entries.tsv` に存在する `person_id`。削除申請の単位も entries 側と共有する |
| `surface` | `ちょこ先` | 表記。愛称の場合は正式名と異なる表記になる |
| `reading` | `ちょこせん` | ひらがな読み（entries と同じ規則） |
| `kind` | `nickname` | 下表の 5 種別のいずれか |
| `note` | `愛称` | 自由記述 |
| `added` | `2026-09-09` | 登録日 |

**`kind` の種別**

| `kind` | `surface` | `reading` | 意味 |
|---|---|---|---|
| `nickname` | 正式名と異なる愛称（`ちょこ先`） | 愛称の読み（`ちょこせん`） | 愛称を変換する |
| `reading_variant` | 正式名そのもの（`癒月ちょこ`） | 通用している別の読み（`ゆずきちょこ`） | 表記揺れ・別読み |
| `family_name` | 正式名の**姓の部分**（`雪花`） | その部分の読み（`ゆきはな`） | 姓だけで変換する |
| `given_name` | 正式名の**名の部分**（`ラミィ`） | その部分の読み（`らみぃ`） | 名だけで変換する |
| `short_name` | **正式名そのもの**（`雪花ラミィ`） | 本人が名乗る短縮名の読み（`らみぃ`） | 短縮名からフルネームへ変換する |

- **正式な読みではない**。IME 辞書としては採用するが、`entries.tsv` の主張（本人・組織公式または出典明示の公開文献）は持たない
- **出典は求めない**。PR での追加も出典 URL は必須にしない
- **本人が望まない愛称は削除申請の対象**。`person_id` 単位で扱うため、正式名の削除申請（Issue）と同じ手続きで、その人物の alias も一緒に消える
- `kind=reading_variant`・`short_name` は、同じ `person_id` の `entries.tsv` に**同じ `surface`** を持つ行が存在すること（別表記に対する別読みは想定しない・そちらは entries 側の別行）
- `kind=family_name`・`given_name` は、`surface` が同じ `person_id` の `entries.tsv` のいずれかの `surface` の**部分文字列**であること（正式名を切り出したものだけを姓・名として扱う）。姓・名の分割の根拠（読みの語区切りとかな境界など）は `note` に書く
- **`reading` が 1 文字の行は登録しない**（1 文字の読みから人名候補が出るのは IME 辞書として有害なため。`surface` だけが 1 文字の行——例: `榊`／`さかき`——は登録できる）
- `entries.tsv` 側で `status=removed` になった `person_id` の alias は、`build.py` の生成物から連動して除外される
- 出力（`dist/`）では entries と合わせて同じ4形式に含める（品詞は entries と同じ「人名」。ATOK のみ「固有人名」）

## 出力形式（生成物・`dist/`）

| IME | 形式 | 品詞 |
|---|---|---|
| MS-IME | UTF-16LE・CRLF・`読み\t表記\t品詞` | 人名 |
| Google 日本語入力 / Mozc | UTF-8・`読み\t表記\t品詞\tコメント` | 人名 |
| ATOK | UTF-16LE・`!!ATOK_TANGO_TEXT_HEADER_1`・`読み\t表記\t品詞` | 固有人名 |
| SKK | EUC-JP or UTF-8・`よみ /表記/` | （品詞なし） |
| macOS 日本語 IM | 未対応 | |

読みに英字が混じる表記（`k4sen`）は読み `かせん` からの変換なので問題ない。

## データの入り方

```
PR / Issue →（メンテナが出典を確認）→ data/entries.tsv・data/aliases.tsv → build.py → dist/
```

- **PR・Issue**: 追加・訂正は PR か Issue で受ける。メンテナが出典 URL を開いて読みを確認してから取り込む
- **メンテナの取り込み**: メンテナが手元でまとめた TSV を `promote.py` で `data/entries.tsv` へ取り込む道具がある
  （`lib/promote.py`・通常は使わない。`needs_review=yes` の行は入れず、既存と読みが食い違えば衝突として止まる）
- **build.py**: entries と aliases から各 IME 形式を生成。`status=removed` を除外。テストは形式ごとのゴールデンファイル
- **CI**: push で build とテスト。tag で Release に dist を添付

## 運用（README・CONTRIBUTING・Issue テンプレ）

- **追加 PR**: entries.tsv に行を足す。`source_url` 必須。レビューは出典を開いて読みを確認する
- **削除・訂正申請 Issue テンプレ**: 本人性の確認は「公式チャンネル・公式 X からの言及」または「所属組織からの連絡」。
  過剰な本人確認は求めない（申請の敷居を下げる方が趣旨に合う）
- **申請時の出典の線**: 正式名（entries）の読みの訂正・追加は出典を必ず入れてもらうが、
  出典は何でも構わない——本人の発言・X の bio・公式ページなど、確認できる URL が 1 つあれば足りる
  （厳格な一次資料は求めない）。ご本人からの申請で URL を示せない場合は、Issue 本文自体を出典として扱う
  （`correction.yml`・`addition.yml` に明記）。
  **愛称・略称（aliases）の追加申請は出典不要**（どこまで確認できるかが難しいため）。
  愛称の追加は `addition.yml`（正式名用）と分け、専用の `alias-addition.yml` を使う
- **削除は人物（`person_id`）単位。removed 人物のどの表記も再登録されない**——別 surface（英字表記・
  愛称由来の別表記など）の行が後から来ても、`promote.py` は追加しない（`lib/promote.py`）
- **収録方針を README 冒頭に**（上の 5 原則）

## 決定事項

1. **リポジトリ**: 単独のリポジトリとして運用する。
2. **ライセンス**: データ CC0 1.0 ／ コード MIT
3. **品詞**: 「人名」で統一（組織名は含めない）
4. **表記の揺れ**: 出典に書かれた**公式表記 1 つ**だけ収録（大小文字違いは別行にしない）
